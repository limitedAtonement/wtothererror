window.onload = function() {
    var container = document.getElementById("the-container");
    var newElement = document.createTextNode("New, DYNAMIC Text");
    container.appendChild(newElement);
};
